services:
    fit-analyzer:
        build:
            context: .
            args:
                GIT_COMMIT: ${GIT_COMMIT:-unknown}
        ports:
            - "3000:3001"
        volumes:
            - fit-data:/app/data
        restart: unless-stopped

volumes:
    fit-data:
